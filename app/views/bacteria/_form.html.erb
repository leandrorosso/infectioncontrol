<%= form_with(model: bacterium) do |form| %>
  <% if bacterium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bacterium.errors.count, "error") %> prohibited this bacterium from being saved:</h2>

      <ul>
        <% bacterium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
